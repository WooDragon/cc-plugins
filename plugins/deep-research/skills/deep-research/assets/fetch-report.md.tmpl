# Fetch Report — {项目名称}

- **采集时间**：`{开始时间}` → `{结束时间}`
- **采集者**：{harvester Task ID / session 标识}
- **数据源**：{系统名称} ({API 类型: REST/GraphQL/CLI})
- **凭证身份**：{认证身份描述，脱敏后}

## 采集统计

| 指标 | 数值 |
|------|------|
| 总对象数 | {N} |
| T1 core 成功 | {N} |
| T2 noncore 成功 | {N} |
| T3 skip 成功 | {N} |
| 失败 | {N} |

## Tier 分层明细

### T1 — 完整采集

| # | id | name | schema fields | views | records | has_more | 备注 |
|---|-----|------|---------------|-------|---------|----------|------|
| 1 | `{id}` | {name} | {N} | {N} | {N} | {Y/N} | |
| ... | | | | | | | |

### T2 — 部分采集

| # | id | name | schema fields | views | records | has_more | 备注 |
|---|-----|------|---------------|-------|---------|----------|------|
| 1 | `{id}` | {name} | {N} | {N} | {N} | {Y/N} | 首页截断 |
| ... | | | | | | | |

### T3 — 仅 schema（skip / 失败）

| # | id | name | schema fields | views | records | 原因 |
|---|-----|------|---------------|-------|---------|------|
| 1 | `{id}` | {name} | {N} | {N} | skipped | test 残留 |
| ... | | | | | | |

## 翻页统计

| 接口类型 | 需翻页对象数 | 已完成翻页 | 翻页截断 | 截断原因 |
|----------|-------------|-----------|---------|---------|
| schema/fields | {N} | {N} | {N} | {原因或"无"} |
| views | {N} | {N} | {N} | |
| records | {N} | {N} | {N} | has_more=true 按策略截断 |

**records has_more=true 对象清单**（{N} 个）：
<!-- 列出所有 has_more=true 的对象，便于 Lead 决定是否补采 -->
- `{id}` ({name})
- ...

## 中英文搜索覆盖率

<!-- 仅 Web 搜索类型研究适用。API 抓取型研究可删除本节或标注"不适用" -->

| 维度 | 中文 | 英文 |
|------|------|------|
| 搜索词数 | {N} | {N} |
| 有效结果数 | {N} | {N} |
| 命中率 | {N}% | {N}% |

**中文搜索词**：{词1}、{词2}、...
**英文搜索词**：{term1}, {term2}, ...

**术语对照表**：

| 中文 | 英文 | 备注 |
|------|------|------|
| {中文术语} | {English term} | |

## 错误汇总

| 错误类型 | 次数 | 涉及对象 | 处理方式 |
|----------|------|----------|----------|
| 429 限流 | {N} | {id 列表} | 指数退避后成功/标记失败 |
| 403 权限不足 | {N} | {id 列表} | 跳过，待补充权限 |
| 5xx 服务端 | {N} | {id 列表} | 重试 {N} 次后成功/失败 |
| 超时 | {N} | {id 列表} | 重试后成功/失败 |
| JSON 解析 | {N} | {id 列表} | 原始响应已保存 |
| 屏蔽源命中 | {N} | {URL 列表} | 跳过 |

<!-- 无错误时写"（无）" -->

## 失败对象详情

<!-- 逐个列出 T3-失败的对象，提供足够信息供 Lead 决策 -->

| id | name | 错误类型 | 重试次数 | 错误信息 | 建议 |
|----|------|---------|---------|---------|------|
| `{id}` | {name} | {type} | {N} | {message} | 补充权限/跳过/手动采集 |

<!-- 无失败时写"（无）" -->

## 候选集完备性（仅选型类研究 / selection）

<!--
仅当 research-goal.md 研究类型 = selection（N 个互斥方案择一）时填写本节。
非选型类研究（综述/趋势/事实核验）→ 本节整体填 "N/A（非选型类研究）"，不强制。
对应 G1 候选集完备性检查（deep-research skill 的 references/quality-gates.md）。
-->

| 项 | 内容 |
|----|------|
| 研究类型 | {selection / non-selection} |
| 候选类别清单 | {已识别的候选方案类别，逐个列出；非选型填 N/A} |
| 候选基数 | {N；selection 类型须 ≥ 3，否则强制补搜} |
| 饱和轮次记录 | {最近 2 轮检索有无新候选类别；无新类别写 "saturation reached after N rounds"；非选型填 N/A} |

## 下一步建议

<!-- harvester 基于采集情况给出建议，供 Lead 在 G1 Gate 决策 -->

1. **补充采集**：{需要补采的对象或数据范围}
2. **策略调整**：{建议调整的翻页策略、限流策略等}
3. **权限补充**：{需要申请的额外权限}
4. **豁免确认**：{需要 Lead 确认的脱敏豁免项}
